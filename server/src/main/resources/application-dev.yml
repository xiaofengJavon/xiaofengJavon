spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:cxzl_platform}?useUnicode=true&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:123456}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      pool-name: HikariPool-Dev

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      database: 0

  jpa:
    show-sql: true

wechat:
  miniapp:
    appid: wx4ed952c6a94c4961
    secret: 75bfa6ea860727e49e3cb9775458320c
  pay:
    appid: wx4ed952c6a94c4961
    mchid: "1480179122"
    mch-key: jemqq8240995jemqq8240995Aa123456
    notify-url: https://422ce83c.r36.cpolar.top/api/v1/payment/callback
    key-path: classpath:cert/apiclient_cert.p12

file:
  upload:
    dir: ./uploads
  base-url: http://localhost:8080

logging:
  level:
    com.subscription.platform: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE
